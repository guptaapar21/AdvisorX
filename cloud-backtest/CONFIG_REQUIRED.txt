Required runtime configuration

USDT_INR_RATE
- Required for TAKE trades.
- Example: 90
- Set to the INR value of 1 USDT used by the workflow.
- If missing/0, Gemini proposals are forced to SKIP rather than using a wrong currency conversion.

Optional safety controls
TAKER_FEE_RATE=0.00075
MIN_RR=1.5
MAX_STOP_PCT=0.08
MIN_STOP_ATR_MULTIPLIER=1.2

There is no MAX_NOTIONAL_INR cap in the current runtime advisor. Position size is controlled by the conviction-based INR loss budget, stop distance, fees, and the other Python risk gates.

Existing required secrets remain required:
GEMINI_API_KEYS (or GEMINI_API_KEY_1..30 / GEMINI_API_KEY)
TELEGRAM_BOT_TOKEN
TELEGRAM_CHAT_ID
