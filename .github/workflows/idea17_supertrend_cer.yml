name: Idea 17 - SuperTrend + VWAP + CER filter (SOL only)

on:
  workflow_dispatch:
    inputs:
      days:
        description: "Lookback window in days"
        required: false
        default: "365"

jobs:
  run:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Install dependencies
        run: pip install -r cloud-backtest/requirements.txt
      - name: Run CER threshold sweep for SOL (one fetch)
        working-directory: cloud-backtest
        run: >
          python run_supertrend_cer_backtest.py
          --coin SOL
          --days ${{ inputs.days }}
          --sweep-all
